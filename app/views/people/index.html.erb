<h1>Listing people</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Lastname</th>
    <th>Email</th>
    <th>Role</th>
   <!-- <th>Inactive</th> -->
    <th>Company</th>
    <th></th>
    <th></th>
  </tr>

<% @people.each do |person| %>
  <tr>
    <td><%= person.name %></td>
    <td><%= person.lastname %></td>
    <td><%= person.email %></td>
    <td><%= person.role %></td>
   <!-- <td><%= person.inactive %></td>  -->
    <td><%= person.company.name unless person.company.nil? %></td>
   <!-- <td><%= link_to 'Show', person %></td>     -->
    <td><%= link_to 'Rediger', edit_person_path(person) %></td>
   <!-- <td><%= link_to 'Destroy', person, :confirm => 'Are you sure?', :method => :delete %></td>-->
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Person', new_person_path %>