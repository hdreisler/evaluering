<%= form_for(@customercompany) do |f| %>
  <% if @customercompany.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@customercompany.errors.count, "error") %> prohibited this customercompany from being saved:</h2>

      <ul>
      <% @customercompany.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :inactive %><br />
    <%= f.check_box :inactive %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
